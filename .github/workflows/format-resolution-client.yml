name: Resolution client format repair

on:
  push:
    branches:
      - chatgpt/resolution-orchestrator-usability

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Apply pinned Prettier output
        run: |
          npx prettier --write \
            apps/extension/src/messaging/native-resolution.test.ts \
            apps/extension/src/messaging/native-resolution.ts \
            packages/application-model/src/resolution-task-parse.test.ts \
            packages/application-model/src/resolution-task-parse.ts
      - name: Commit canonical formatting when needed
        run: |
          if git diff --quiet; then
            echo "No formatting changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            apps/extension/src/messaging/native-resolution.test.ts \
            apps/extension/src/messaging/native-resolution.ts \
            packages/application-model/src/resolution-task-parse.test.ts \
            packages/application-model/src/resolution-task-parse.ts
          git commit -m "style: apply canonical resolution client formatting"
          git push origin HEAD:chatgpt/resolution-orchestrator-usability
