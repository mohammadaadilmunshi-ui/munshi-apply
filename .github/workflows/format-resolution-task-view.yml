name: Resolution task view format repair

on:
  push:
    branches:
      - chatgpt/resolution-orchestrator-usability

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Apply pinned Prettier output
        run: |
          npx prettier --write \
            apps/extension/src/sidepanel/resolution-task-view.ts \
            apps/extension/src/sidepanel/resolution-task-view.test.ts
      - name: Commit canonical formatting
        run: |
          if git diff --quiet; then
            echo "No formatting changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            apps/extension/src/sidepanel/resolution-task-view.ts \
            apps/extension/src/sidepanel/resolution-task-view.test.ts
          git commit -m "style: format resolution task queue view"
          git push origin HEAD:chatgpt/resolution-orchestrator-usability
